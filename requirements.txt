# Core dependencies
imagehash>=4.3.0
pillow>=9.0.0
tqdm>=4.62.0

# For feature-based verification (ORB)
opencv-python>=4.5.0
numpy>=1.20.0

# Optional: for GPU acceleration
torch>=1.12.0; platform_system=="Linux" or platform_system=="Darwin" or platform_system=="Windows"

# Development dependencies (optional)
pytest>=7.0.0; extra == "dev"
